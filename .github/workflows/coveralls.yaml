name: Coveralls
on:
  push:
    branches:
      - 'main'
    paths:
      - '**.ts'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  coveralls:
    uses: christiangalsterer/github-actions/.github/workflows/bun_coveralls.yaml@main
