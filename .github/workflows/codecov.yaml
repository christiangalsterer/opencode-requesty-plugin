name: Codecov
on:
  push:
    branches:
      - 'main'
    paths:
      - '**.ts'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  codecov:
    uses: christiangalsterer/github-actions/.github/workflows/bun_codecov.yaml@main
    secrets: inherit
